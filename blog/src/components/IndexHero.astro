---
import Socials from "@/components/Socials.astro";
import LinkButton from "@/components/LinkButton.astro";
import { SITE } from "@/config";
import { SOCIALS } from "@/constants";
---

<!-- md以上なら横に2分割 -->
<div class="border-bolder mb-6 grid gap-8 border-b pb-4 md:grid-cols-5">
  <section id="hero" class="py-4 md:col-span-3">
    <h2 class="my-2 inline-block text-4xl font-bold sm:my-4 sm:text-2xl">
      About This Blog
    </h2>
    <p>
      自分用のメモ。C#の内容を中心に、目標や学習記録を書いていきます。<br />
      一部記事はそのまま
      <LinkButton
        href="https://zenn.dev/arika"
        target="_blank"
        class="underline-offset-4 hover:underline"
      >
        Zenn
      </LinkButton>
      にも掲載されるようにしています。<br />
    </p>
  </section>
  <section id="authors" class="py-4 md:col-span-2">
    <h2 class="my-2 inline-block text-4xl font-bold sm:my-4 sm:text-2xl">
      Author
    </h2>
    <div class="flex flex-col items-center gap-4 sm:flex-row">
      <img
        src="https://avatars.githubusercontent.com/u/4524647?v=4"
        alt="arika0093"
        class="mb-2 h-24 w-24 rounded-full object-cover"
      />
      <div class="flex w-full flex-1 flex-col items-start">
        <div class="font-medium">
          <a target="_blank" href={SITE.profile}>{SITE.author}</a>
        </div>
        <div class="text-sm opacity-80">
          Full-stack engineer(多分) <br />
          またの名を雑用係
        </div>
        {
          // only display if at least one social link is enabled
          SOCIALS.length > 0 && (
            <div class="mt-2 flex max-sm:flex-col sm:items-center">
              <Socials />
            </div>
          )
        }
      </div>
    </div>
  </section>
</div>
