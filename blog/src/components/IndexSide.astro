---
import Socials from "@/components/Socials.astro";
import LinkButton from "@/components/LinkButton.astro";
import { SITE } from "@/config";
import { SOCIALS } from "@/constants";
import PinArticles from "./PinArticles.astro";
---

<div id="side" class="mt-8 md:mt-0 md:w-[350px]">
  <section>
    <h2 class="inline-block text-2xl font-semibold">
      About This Blog
    </h2>
    <p class="opacity-80 mt-3">
      自分用のメモ。C#の内容を中心に、目標や学習記録を書いていきます。
      一部記事はそのまま
      <LinkButton
        href="https://zenn.dev/arika"
        target="_blank"
        class="underline-offset-4 hover:underline"
      >
        Zenn
      </LinkButton>
      にも掲載されるようにしています。<br />
    </p>
  </section>
  <section id="authors" class="py-4">
    <h2 class="my-4 inline-block text-2xl font-semibold sm:text-xl">
      Written by
    </h2>
    <div class="flex flex-row items-center gap-4">
      <img src="https://avatars.githubusercontent.com/u/4524647?v=4" alt="arika0093" class="mb-2 h-20 w-20 rounded-full object-cover" />
      <div class="flex flex-col items-start w-full flex-1">
        <div class="font-medium">
          <a target="_blank" href={SITE.profile}>{SITE.author}</a>
        </div>
        <div class="text-sm opacity-80 mt-1">
          北関東在住のプログラマー
        </div>
        {
          // only display if at least one social link is enabled
          SOCIALS.length > 0 && (
            <div class="mt-2 flex max-sm:flex-col sm:items-center">
              <Socials />
            </div>
          )
        }
      </div>
    </div>
  </section>
  <PinArticles />
</div>